services:
  - type: web
    name: kite-conditions
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn kite_web:app --bind 0.0.0.0:$PORT --workers 2 --threads 2
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    plan: free
